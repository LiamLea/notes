1.首先导入环境变量（bootrc)
2.解压所有的tar包
3.apt/sit.sh
  设置安装源，并安装python-pip,docker-ce,并启动docker
4.server/sit.sh -> util.sh
(1)spawn_network
(2)set_env
(3)install_docker_compose
(4)up_servers
(5)deploy_docker_certs


kolla@nexus:~$ sudo mkdir /opt/dire
kolla@nexus:~$ sudo tar -xf boot.tar.gz 
kolla@nexus:~$ sudo mv ~/packages/ /opt/dire/
kolla@nexus:~$ sudo mv ~/boot /opt/dire/
kolla@nexus:~$ sudo vim /opt/dire/boot/tools/bootrc
kolla@nexus:~$ cd /opt/dire/boot/
kolla@nexus:/opt/dire/boot$ sudo bash tools/deploy.sh 
